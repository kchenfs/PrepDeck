// src/App.tsx
import { DashboardPage } from './pages/DashboardPage';

function App() {
  // In the future, this component will handle routing between
  // the LoginPage, DashboardPage, and HistoryPage.
  // For now, it just shows the dashboard.
  return <DashboardPage />;
}

export default App;
